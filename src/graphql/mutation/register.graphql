mutation Register($username: String!, $password: String){
    register(optiuons: { username: $username, password: $password})
    {
        errors{
            field
            message
        }
        user{
            id
            username
        }
    }
}